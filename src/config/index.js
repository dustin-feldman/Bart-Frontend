const config = {
  baseUrl: import.meta.env.VITE_API_END_POINT || '',
  dashboardPath: '/sample-page',
  defaultConfig: {
    fontFamily: `'Roboto', sans-serif`,
    borderRadius: 8
  }
};

export default config;
